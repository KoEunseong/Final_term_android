<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.final_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".join" android:label="join 액티비티"/>
        <activity android:name=".MainActivity" android:label="Login 액티비티"/>
        <activity android:name=".FindStudy" android:label="findStudy 액티비티"/>
        <activity android:name=".Profile" android:label="profile 액티비티"/>
        <activity android:name=".Search" android:label="search 액티비티"/>
        <activity android:name=".Post_regist" android:label="Post_regist 액티비티"/>
        <activity android:name=".MyPage" android:label="MyPage 액티비티"/>
        <activity android:name=".Study_record" android:label="study_record 액티비티"/>
    </application>

</manifest>